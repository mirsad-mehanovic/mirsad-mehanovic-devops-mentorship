#!/bin/bash
# testing two command line parameters with multiple inputs sent while running the script

total=$[ $1 * $2 ]

echo The first parameter is $1.
echo The second parameter is $2.
echo The total value when multiplied is $total.
